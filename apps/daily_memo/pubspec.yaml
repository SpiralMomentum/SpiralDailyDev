name: apps.daily_memo
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.7+14

environment:
  sdk: '>=3.0.6 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  go_router: ^6.0.1
  sqflite:
  path:
  rxdart: ^0.27.7
  intl: ^0.18.0
  table_calendar: ^3.0.9
  freezed_annotation:
  json_serializable:
  equatable: ^2.0.3
  flutter_bloc: ^8.1.1
  core_app_shell:
    path: ../../packages/core/app_shell
  core_di:
    path: ../../packages/core/di
  core_navigation:
    path: ../../packages/core/navigation

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build_runner: ^2.2.0 # For generating code (Mocks, etc)
  mockito: ^5.2.0
  freezed:

flutter:
  uses-material-design: true

