name: world_map_widget
description: Reusable world map widget built on top of countries_world_map.
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ^3.10.0
  flutter: '>=3.7.0'

dependencies:
  flutter:
    sdk: flutter
  countries_world_map: ^1.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
