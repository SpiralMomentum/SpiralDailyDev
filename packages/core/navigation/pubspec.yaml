name: core_navigation
description: Shared navigation abstractions for SpiralDailyDev apps.
version: 0.0.1
publish_to: "none"

environment:
  sdk: '>=3.0.6 <4.0.0'
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  go_router: ^6.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
