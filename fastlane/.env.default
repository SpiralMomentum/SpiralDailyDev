# Non-secret default environment variables for Fastlane
# Secrets (ASC_KEY_ID, ASC_ISSUER_ID, ASC_KEY_CONTENT, etc.) must be set via CI or local env.

FASTLANE_SKIP_UPDATE_CHECK=1
FASTLANE_HIDE_CHANGELOG=1
