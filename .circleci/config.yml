version: 2
jobs:
  build:
    working_directory: ~/app
    docker:
      - image: cirrusci/flutter:3.3.10
    steps:
      - checkout
      - run: flutter packages get
      - run: flutter pub global activate melos
      - run: melos bootstrap
      - run: melos daily memo pub get