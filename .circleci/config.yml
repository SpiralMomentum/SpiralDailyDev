version: 2
jobs:
  build:
    working_directory: ~/spiral_dev
    docker:
      - image: cirrusci/flutter:3.3.10
    steps:
      - checkout
      - run: flutter packages get
      - run: flutter pub global activate melos
      - run: melos bootstrap
      - run:
          name: melos daily memo pub get
          command: melos daily memo pub get
          working_directory: ~/app