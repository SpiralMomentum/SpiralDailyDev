version: 2

jobs:
  build:
    docker:
      - image: cirrusci/flutter:3.3.10
    steps:
      - checkout
      - run: export PATH="$PATH:/path/to/flutter/bin"
      - run: flutter pub global activate melos
      - run: melos bootstrap
      - run: melos daily_memo_pub_get
      - run: melos exec -- flutter test